const pako=require("pako");
const fzstd=require("fzstd");
const vdata = require ('../lib/index.js');
const fs = require('fs');

test('simple decode test', () => {
       var ab= fs.readFileSync('/Users/garyshi/Documents/gitwork/vdatashj/diff_freq_0.vsw');
       let reader = new  vdata.VDataByteReader(ab);
       let vreader = new  vdata.VDataReader([reader]);
       let frame = vreader.df();
       let vals = frame.objects();
       let cols = frame.cols(true);
       expect(vals.length).toBeGreaterThan(2);
    expect(true).toBe(true);
});
